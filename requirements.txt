# ======================================================
# ContractIQ — Full Requirements (single venv)
# Python 3.11
#
# Install order:
#   1. pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
#   2. pip install -r requirements.txt
#   3. pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
# ======================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.22

# --- PDF generation ---
reportlab==3.7.4

# --- Core LangChain stack ---
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14
langchain-groq==0.2.3
langchain-huggingface==0.1.2
langgraph==0.2.62

# --- Vector DB ---
chromadb==1.5.5

# --- Embeddings & Transformers ---
sentence-transformers==3.3.1
transformers==4.47.1
tokenizers==0.21.0

# --- NLP / spaCy ---
spacy==3.8.3
# spaCy model installed separately — see step 3 above

# --- PII detection ---
presidio-analyzer==2.2.356
presidio-anonymizer==2.2.356
presidio-analyzer[transformers]
# --- Document processing ---
docling==2.14.0

# --- Fuzzy matching ---
thefuzz==0.22.1
python-Levenshtein==0.26.1

# --- Data / utilities ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
numpy==1.26.4
pandas==2.2.3
httpx==0.28.1

# --- torch is NOT listed here ---
# Must be installed first with its own index URL (see step 1 above)
# pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu